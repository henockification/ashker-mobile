import { View } from 'react-native';

/** Full-bleed thick band between profile menu sections (matches padded layout). */
export function MeSectionSpacer() {
  return <View className="-mx-5 h-4 bg-neutral-100" />;
}
